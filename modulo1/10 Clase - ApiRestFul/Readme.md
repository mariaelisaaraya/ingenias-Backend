Ejecuta en la ventana Terminal NPM INSTALL para que se agreguen las dependencias y se genere la carpeta node_modules.

Crea el archivo .env y agrega en éste lo siguiente:

PORT=3008
DATABASE_PATH=/database/frutas.json

